//import of necessary libraries
import java.awt.*;

//nessesary variable
interface Variable{
	int f_width=100;
	Color[] colors = {Color.RED, Color.GREEN, Color.BLUE, Color.MAGENTA};
	String[] colorNames = {"RED", "GREEN", "BLUE", "MAGENTA"};
	Color background = new Color(238, 238, 238);
	Font standartFont = new Font("Dialog", Font.PLAIN, 12);
}
